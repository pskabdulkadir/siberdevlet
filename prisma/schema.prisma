// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Bot {
  id                String    @id @default(cuid())
  name              String
  role              String
  ministry          String
  status            String
  energy            Float
  balance           Float
  performanceScore  Float
  createdTick       Int
  logs              String    @default("[]")
  
  // Skill Matrix
  skillExtraction   Float
  skillGeneration   Float
  skillRefinement   Float
  skillCrafting     Float
  skillPricing      Float
  skillCoding       Float
  skillArchitecture Float
  skillRegulation   Float
  skillInspection   Float
  skillGateway      Float
  
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
  
  @@map("bots")
}

model DigitalAsset {
  id                String    @id @default(cuid())
  title             String
  type              String
  content           String
  creatorId         String
  creatorName       String
  price             Float
  sold              Boolean   @default(false)
  buyerId           String?
  timestamp         BigInt
  
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
  
  @@map("digital_assets")
}

model LedgerTransaction {
  id                String    @id @default(cuid())
  fromId            String
  fromName          String
  toId              String
  toName            String
  amount            Float
  purpose           String
  timestamp         BigInt
  
  createdAt         DateTime  @default(now())
  
  @@map("ledger_transactions")
}

model Job {
  id                String    @id @default(cuid())
  queueName         String
  name              String
  status            String
  data              String    @default("{}")
  result            String?
  progress          Float
  workerId          String?
  error             String?
  timestamp         BigInt
  
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
  
  @@map("jobs")
}

model SimulationMeta {
  id                String    @id @default("singleton")
  activeTicks       Int
  totalGAIA         Float
  subsidyPool       Float
  inflationRate     Float
  taxRate           Float
  interestRate      Float
  resilienceScore   Float
  serverCpu         Float
  serverRam         Float
  chaosEvents       Int
  evolutionGeneration Int
  recycledBotCount  Int
  marketVolume      Float
  rateLimitRisk     Float
  proxyRotations    Int
  activeProxy       String?
  geminiMode        String
  geminiQuotaExhausted Boolean
  autoPayoutThreshold String
  ownerIban         String?
  ownerCryptoWallet String?
  
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
  
  @@map("simulation_meta")
}

model BackupLog {
  id                String    @id @default(cuid())
  snapshotData      String
  backupSize        Int
  success           Boolean
  errorMessage      String?

  createdAt         DateTime  @default(now())

  @@map("backup_logs")
}

// v21.0: WALLET HAVUZU - KALICI DEPOLAMA
model WalletPool {
  id                String    @id @default("singleton")
  totalUSD          Float     @default(0)
  totalTRY          Float     @default(0)
  totalTransactions Int       @default(0)
  lastUpdate        BigInt    @default(0)

  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  @@map("wallet_pool")
}

// v21.0: WALLET İŞLEM KAYITLERI
model WalletTransaction {
  id                String    @id @default(cuid())
  poolId            String    @default("singleton")
  amount            Float
  amountTRY         Float
  source            String    // "Dış Satış", "Marketplace", vb
  status            String    @default("pooled") // pooled, transferred
  orderId           String    @unique
  timestamp         BigInt

  // Transfer bilgileri (varsa)
  transferId        String?   @unique
  walletAddress     String?
  txHash            String?
  transferStatus    String?   // pending, success, failed

  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  @@map("wallet_transactions")
}
